## Implementation

- [x] Update startup citizen seeding to distribute across map roads.
- [x] Enforce non-road occupancy checks for spawn candidate acceptance.
- [x] Add/adjust tests for road placement, non-overlap, and spread.
- [x] Validate with `openspec validate fixed-npc-population --strict`, `cmake --build build`, and `rtk test ctest --test-dir build --output-on-failure`.
